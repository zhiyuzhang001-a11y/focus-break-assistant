# 下一步执行计划

> 生效日期：2026-09-04  
> 当前主线：完成 Stage 1 视觉提醒原型；Stage 0 长时资源测试作为后台验收并行进行。

## 推进原则

1. 长时测试不阻塞可以安全并行的开发；只有发现资源异常时才插入修复。
2. 当前不实现完整计时器、行为引擎、设置页或数据报表，防止视觉问题与提醒时机问题混在一起。
3. 自动化和无需打断用户的验证由 Codex 直接完成；真实硬件与真实试用者依赖单独列出，不伪装为已验证。

## 工作流 A：关闭 Stage 0 残余项（后台）

- [ ] 读取 8 小时批次的完整汇总，检查 RSS 趋势、峰值、平均 CPU、异常退出和错误日志；
- [ ] 将 `/Users/Shared/FocusBreakAssistant-Test` 的结果归档回项目；
- [ ] 若指标稳定，记录 Stage 0 风险接受并关闭该阶段；
- [ ] 若指标异常，只修复明确的资源问题并重跑针对性测试，不回退已经通过的视觉工作；
- [ ] 多显示器实机和干净账户首次启动继续延期至硬件可用或发布前。

完成条件：长时批次正常完成、无持续内存增长或异常 CPU，并在阶段记录中明确仍未覆盖的 Energy Trace 和硬件项。

## 工作流 B：完成 Stage 1（当前主线）

### B1. 视觉基线——已完成

- [x] `252 × 76 pt` Horizon Gap 浮层；
- [x] 固定两行文案和克制配色；
- [x] 13/14 pt 对照，暂定 14 pt 为默认；
- [x] 浅色、深色、复杂背景和代码界面视觉矩阵；
- [x] 不抢焦点、点击穿透、不可成为 key/main window 的自动验证。

该基线仅保留为工程验证。用户评审确认其过于简单且类似通知，不作为最终视觉方案。

### B1.5 体验优先的视觉重构——当前

- [x] 确认旧版通知卡片过于简单且容易被误认；
- [x] 生成远山雾幕、抽象光隙和水墨天幕三版探索稿；
- [x] 纠正“从三种画风中选一个”的错误收窄，恢复以眼睛友好、低打扰、余白和离屏意图为约束；
- [x] 从抽象光与空间、材质与留白、时间与停顿等开放类别收集首批 `9` 个来源候选；
- [x] 将钟表、沙漏、流水、地球运动、落花/落叶、日出和生命过程纳入概念池，并记录首轮风险排序；
- [ ] 对每个网络素材确认无水印、原始来源及 App 使用/修改授权，并建立来源记录；
- [x] 用三个代表性官方来源完成首轮真实工作界面合成，并根据亮度和占屏高度完成一次收敛；
- [x] 完成时间器物、流水、落叶和日出四张新增概念对照；
- [ ] 结合用户反馈和真实桌面测试，从七张开放概念中筛至最多 `3` 个体验候选；
- [ ] 用户确认体验方向，而不是被要求选择山水、水墨或既有方向稿；
- [ ] 制作无文字、无桌面内容的深浅两套正式氛围资产；
- [ ] 用原生 Swift 实现准确文案和符合减少动态效果设置的出现/消散动画。

### B2. 真实运行环境验证——下一步

- [ ] 在当前真实桌面和代码工作界面各预览一次；
- [ ] 与一条系统通知并发，检查空间重叠和视觉误认；
- [x] 用通知参考场景验证固定布局的空间分离（仅为软件场景，不替代上一项真实通知）；
- [ ] 校验当前显示缩放下的屏幕边缘、菜单栏和通知安全区域；
- [ ] 重新检查减少动态效果、增强对比度和减少透明度分支；
- [ ] 将结果和截图写入 Stage 1 记录。

完成条件：文字清晰、提醒不覆盖通知、不改变前台应用、不阻塞输入。

### B3. 可用的预览入口

- [x] 把现有 `--preview-reminder` 接入一个预览专用的菜单栏入口；
- [x] 保持原型只负责“预览一次提醒”，不提前接入完整计时和状态机；
- [x] 增加构建与启动脚本，并做签名、启动和退出回归。

完成条件：用户无需终端即可触发一次预览，且菜单栏入口不会改变提醒本身的无交互原则。

运行时验收曾发现 `.accessory` 激活策略会短暂抢占前台，已改为 `.prohibited`；当前验证结果为菜单项存在、图标使用模板色、前台应用不变、浮层点击穿透。

### B4. 结构化反馈

- [x] 准备固定三问反馈模板；
- [ ] 邀请 5–8 名试用者体验一次或少量重复预览；
- [ ] 汇总“突然被打断 / 是否看清 / 是否误认通知或任务完成”三项结果；
- [ ] 最多进行两轮有针对性的视觉调整，避免无目标美化。

完成条件：多数试用者理解“短暂离屏”，文字可读，且不会普遍误认成系统通知或任务完成提示。

## Stage 1 之后

Stage 1 达到退出条件后进入 Stage 2，只实现确定性行为引擎 MVP：单调计时、自然休息取消、全屏延后、同周期一次提醒、睡眠/重启不补发。先完成状态转换表和模拟时间测试，再连接真实系统信号与浮层。

## 当前无需用户处理的事项

Codex 可以继续完成资源结果验收、真实本机视觉验证、预览菜单栏原型和自动回归。只有以下两项需要外部条件：5–8 名真实试用者反馈，以及第二台显示器/其他尺寸 Mac 的实机验证。
