# 体验改进与夜间验收 · 2026-09-07

## 最终结果 · 2026-09-08 08:49

功能改进已经打包并运行；46 项测试与自动视觉检查通过。正式夜间程序于 08:48:40 写入 finished 后退出，记录 43,200.241 秒（12 小时）、8,641 次定时采样和 721 条分钟样本。主应用仍在运行。

- 真实无人值守通道：全程已记录分钟样本的累计使用量为 0，真实提醒 0 次，验证窗口数均为 0；符合无人操作不累计工作时间的预期。这不是真人连续使用验收。
- 注入活动的真实墙钟通道：约 45 分钟触发一次，12 小时内没有重复。没有拼接开发预热时间。
- 真实跨天图库更新：六类在 00:04:04–00:06:59 完成新批次提交，各 10 张、共 60 张，最终文件齐全；至结束没有再次提交。总容量 18,797,130 字节（17.93 MiB），单类均低于 20 MiB。未测得更新过程的瞬时峰值。
- 进程资源：主应用最终 RSS 56,640 KiB、即时 CPU 0.0%；跨天更新后的逐小时 RSS 为 56,736→56,688→56,640 KiB，未见这些采样中的持续增长。不是多日内存或全程功耗保证。
- 真实显示器睡眠通知出现一次，未出现唤醒、系统睡眠、会话切换或物理多屏变化，因此不把相关完整流程标为通过。真实断网恢复、注销/登录、真人连续使用和多日运行仍需人工验收。

夜间检查未发现需要修复的实质故障。本轮自动跟进将暂停，主应用继续正常运行；无需为查看结果再次开启夜间测试。

## 已实现

- 设置分为提醒、图片库、通用；菜单顶层仍为状态、暂停、预览、设置、退出。
- 状态区显示倒计时、暂停剩余时间、休息倒计时，以及全屏、暂离、设置占用、屏幕状态未知等延后原因。
- 提醒提供开始休息（5 分钟）、稍后 5 分钟、跳过本次、收起与 Esc；默认停留 30 秒，可设 15/30/60/120 秒。短/窄图片将操作收进紧凑菜单。
- 收藏复制原始字节和来源许可到独立 PersonalCollection/Favorites，自动更新不清理收藏。可以查看记录、取消收藏。
- 屏蔽按来源 ID 和文件摘要持久保存，显示时排除、更新时跳过；支持单张或全部恢复。缓存更新恰好移走当前图片时，仍能收藏或屏蔽已显示的图片。
- 在线批次过滤近似画面及相同标题组；轮换优先隔开相似图片，但仍保留一轮用完再洗牌的规则。人物连拍序号可归组，天文目标编号不会被误删。这是启发式去重，不是人脸或人物身份识别，不保证识别所有同一人物。
- 文字支持中央、下方与简短模式，使用字形描边与阴影增强对比，背景保持透明。未增加分类、未生成图片。
- 默认沿用上次分类；在菜单倒计时的子菜单中直接多选切换，不打开窗口。完整每日选择页默认关闭，仍可从设置启用或主动打开。
- 屏幕布局变化时收起旧浮层，下次按当前屏幕重新布局。偏好保存失败有错误反馈，不改动用户原图；偏好清单损坏时保留原文件并停止覆盖。

## 已执行的验证

- 最新 `swift test`：46 项、17 组全部通过。见 `artifacts/experience-tests-20260907.log`。
- 测试覆盖稍后/暂停/恢复、跳过、显式休息、收藏跨缓存删除与重启、屏蔽下载、损坏偏好保护、模拟断网保留整批旧图、本地自然日跨天和失败重试限流、分类偏好持久化、轮换去重。
- AppKit 测试覆盖设置三页布局、操作按钮、Esc 响应及重复关闭不重复回调。实机检查修正了滚动内容靠下排列的问题，已确认设置内容从顶部排列。
- 原有自动视觉检查通过，含 18 张现成照片界面渲染与照片格式/轮换/回退验证。目录：`artifacts/stage1-visual-20260907-204624`。
- 最终打包成功并通过 `codesign --verify --deep --strict`。最后的菜单实测结果为 `artifacts/final-preview-menu-20260907.json`：五个顶层项、交互可用、前台应用保持不变。此前一次与应用启动同时进行的采样受到前台应用切换影响，已在启动稳定后重新独立验证。
- 已重新启动最新版 `.build/FocusBreakProbe.app`，当前为正常菜单栏运行，未打开设置阻塞提醒。

## 已完成的真实夜间记录

- 正式开始：北京时间 2026-09-07 20:48:39；计划到北京时间 2026-09-08 08:48:39 结束。
- 固定测试程序：`.build/overnight-runtime-20260907/focus-break-probe`。复制到独立目录，后续构建不会改写正在测试的可执行文件。
- 正式日志：`artifacts/overnight-verification-20260907.jsonl`，开始记录 PID 85561。此前约 11 分钟的开发预热日志另存 `artifacts/overnight-warmup-20260907.jsonl`，不拼接成正式 45 分钟结果。
- 真实信号通道：使用真实空闲/睡眠/会话信号及 45 分钟阈值。开始时已空闲约 20 分钟，正确保持累计为 0。无人操作不算连续使用测试通过。
- 独立计时通道：每 5 秒用真实单调时钟推进，但注入“正在使用”样本。它可验证真实等待 45 分钟的定时精度，不能替代真人连续使用验收；不向系统合成键鼠输入，不阻止机器睡眠。
- 每分钟记录计时、空闲、窗口数；记录睡眠/会话通知、显示器拓扑变化和本地跨天事件。
- 图库跨天前基线：`artifacts/overnight-library-before-20260907.json`。自动跟进需对比第二天的六类 active.json 时间、数量和磁盘容量；保留旧批时如实记录，不直接判为更新成功。
- 资源基线：`artifacts/overnight-resource-baseline-final-20260907.txt`。后续检查应追加实际 CPU/RSS 和进程存活证据，不从短时结果推断多天稳定性。
- 本任务已安排每小时自动跟进，最多 13 次；正式日志完成或发现失败后整理结果并暂停跟进。机器/任务调度不可用时只能在恢复后继续，不能保证睡眠期间执行。

## 仍需真实场景验收，不能提前勾选

- [ ] 真人连续使用 45 分钟，观察提醒一次、稍后、跳过与休息的完整体验。
- [ ] 真实睡眠唤醒、锁屏、用户会话切换；仅记录到真实事件才能作为对应证据。
- [ ] 真实拔插多屏、竖屏、超宽屏，检查图片、文字与操作区域；几何单元测试不是物理设备测试。
- [ ] 实际断网恢复；当前只有注入断网错误的确定性测试，没有主动关闭网络。
- [ ] 登录启动的真实注销/重新登录；没有替用户注销或启用登录启动。
- [ ] 多日功耗和内存稳定性；一晚日志不能代替多日验收。

## 后续自动跟进记录

以下为逐小时实际检查记录，最终结论见文首。

### 2026-09-07 21:42（北京时间）

- 两个进程均存活，约运行 53 分钟。采样 RSS：正式验证程序 31,376 KiB、主应用 43,616 KiB；即时 CPU 均 0.0%。这是单次采样，不等于全程 CPU 为零。
- 注入活动的墙钟通道于 21:33:39 触发一次，记录 elapsedWallSeconds=2699.994；截至 21:40:39 未重复触发。该约 45 分钟结果仅证明注入通道的真实计时，不是真人连续使用验收。
- 真实活动通道累计为 0、提醒数为 0，最新空闲约 4,323 秒；符合无人操作不计入连续工作的规则。
- 20:48:48 收到真实 display_sleep_notification；尚未记录唤醒，不勾选完整睡眠/唤醒验收。
- 尚未跨天，也未出现 finished；保持夜间任务与自动跟进运行。原始检查快照已保存到 artifacts/overnight-checkpoints-20260907.jsonl。

### 2026-09-07 22:41（北京时间）

- 两个进程继续存活，已运行约 1 小时 52 分钟；RSS 分别为 31,376 KiB 和 43,616 KiB，与上一小时采样一致，即时 CPU 均为 0.0%。
- 最新正式日志为 22:40:39：真实通道累计 0、提醒 0；注入通道累计 2700 秒、提醒仍为 1。所有已记录分钟样本的验证进程窗口数为 0。
- 没有新增睡眠/唤醒或显示器拓扑事件，尚未跨天、尚未结束；继续保留夜间进程和自动跟进，不把空闲时间算作真人连续使用验收。

### 2026-09-07 23:42（北京时间）

- 两个进程已连续运行约 2 小时 53 分钟；采样 RSS 仍分别为 31,376 KiB、43,616 KiB，即时 CPU 均 0.0%，未见本次采样中的增长。
- 最新样本 23:41:40：真实通道累计 0、提醒 0、空闲约 11,583 秒；注入通道仍只触发一次，窗口数为 0。没有新增系统/拓扑事件。
- 尚未跨本地自然日，日志尚无 finished。保留现有进程，下一轮跨天后再比较六类图库的实际批次更新证据；未将无人值守时间算作真人使用验收。

### 2026-09-08 00:43（北京时间）

- 正式日志在 00:00:00 记录真实本地跨天事件。六类 active.json 相比基线均换为新的批次目录，实际提交时间为 00:04:04 至 00:06:59；各 10 张、共 60 张，清单所列文件均存在。这次是实际跨天整批更新成功，并非保留旧批。
- 六类目录合计 18,797,130 字节（约 17.93 MiB）；单类最大约 4.40 MiB，当前采样均在每类 20 MiB 限额内。该快照不能证明更新期间的瞬时峰值。明细：artifacts/overnight-library-after-20260908-0042.json。
- 两个进程存活约 3 小时 54 分钟；验证程序 RSS 31,376 KiB、主应用 56,736 KiB，即时 CPU 均 0.0%。主应用较此前采样增加 13,120 KiB，发生在跨天联网更新之后；仅记录相关时间，尚不能据此判定原因或泄漏，后续继续比较。
- 最新真实通道累计 0、提醒 0；注入通道仍只触发 1 次，验证窗口数为 0。没有完整睡眠/唤醒或物理多屏切换证据，日志尚未 finished，继续运行。

### 2026-09-08 01:44（北京时间）

- 两个进程存活约 4 小时 56 分钟；RSS 仍为 31,376 KiB 和 56,736 KiB，与跨天后上一轮采样一致，即时 CPU 均为 0.0%。未在本轮发现继续增长。
- 六类仍各 10 张、无缺失文件，总容量维持 18,797,130 字节；批次提交时间与 00:42 快照相同，没有再次更新的迹象。明细已保存到 artifacts/overnight-library-after-20260908-0144.json。
- 最新日志 01:43:40：真实提醒为 0，注入提醒仍为 1，窗口数为 0；没有新增系统事件，尚未 finished。继续夜间记录，不扩大真人验收结论。

### 2026-09-08 02:44（北京时间）

- 两个进程持续存活约 5 小时 55 分钟；验证程序 RSS 31,376 KiB、主应用 56,736 KiB，与上一小时一致，即时 CPU 均 0.0%。
- 六类仍保持跨天后批次，各 10 张且文件齐全；提交时间及总容量 18,797,130 字节均未变化。快照：artifacts/overnight-library-after-20260908-0244.json。
- 最新日志 02:43:40：真实累计和提醒仍为 0，注入提醒仍为 1，验证窗口数为 0。事件总数仍为 5，无新增系统事件或 finished。按计划继续，不提前结束夜间验收。

### 2026-09-08 03:44（北京时间）

- 两个进程存活约 6 小时 55 分钟，RSS 仍分别为 31,376 KiB、56,736 KiB，即时 CPU 均 0.0%；本轮没有发现内存继续增长。
- 六类实际跨天批次仍各 10 张、无缺失文件；提交时间不变，总容量仍为 18,797,130 字节。快照：artifacts/overnight-library-after-20260908-0344.json。
- 最新日志 03:43:40：真实累计/提醒为 0，注入提醒为 1，验证窗口数为 0。没有新增系统或拓扑事件，尚未 finished；保留进程及自动跟进至正式结束。

### 2026-09-08 04:44（北京时间）

- 两个进程存活约 7 小时 55 分钟；RSS 仍分别为 31,376 KiB、56,736 KiB，即时 CPU 均为 0.0%，与上一轮采样一致。
- 六类跨天批次仍各 10 张、文件齐全；提交时间不变，总容量仍为 18,797,130 字节。快照：artifacts/overnight-library-after-20260908-0444.json。
- 最新样本 04:43:41：真实累计/提醒为 0，注入提醒为 1，验证窗口数为 0；事件总数仍为 5，无新增事件或 finished。继续运行至计划结束，不将这些无人值守结果记为真人使用验收。

### 2026-09-08 05:44（北京时间）

- 两个进程已运行约 8 小时 56 分钟并仍存活；采样 RSS 分别为 31,376 KiB、56,736 KiB，即时 CPU 均 0.0%，与上一轮一致。
- 六类跨天批次仍各 10 张且文件齐全，更新时间无变化，总容量保持 18,797,130 字节。明细：artifacts/overnight-library-after-20260908-0544.json。
- 最新样本 05:43:42：真实累计/提醒为 0，注入提醒为 1，验证窗口数为 0；无新增系统事件或 finished。正式运行继续，真人使用和完整睡眠/唤醒等项目仍待验收。

### 2026-09-08 06:47（北京时间）

- 两个进程存活约 9 小时 58 分钟；RSS 仍分别为 31,376 KiB、56,736 KiB，即时 CPU 均为 0.0%，与上一轮采样一致。
- 六类跨天后批次各 10 张且文件齐全，提交时间与总容量 18,797,130 字节均未变化。快照：artifacts/overnight-library-after-20260908-0646.json。
- 最新样本 06:46:42：真实累计/提醒为 0，注入提醒为 1，验证窗口数为 0；无新增系统事件或 finished。离计划结束约两小时，继续保留运行与自动跟进，不提前认定整夜验收完成。

### 2026-09-08 07:49（北京时间）

- 两个进程持续存活约 11 小时；验证程序 RSS 31,376 KiB，主应用 56,688 KiB（比上一轮少 48 KiB），即时 CPU 均 0.0%，未见本轮持续增长。
- 六类跨天批次仍各 10 张、文件齐全，更新时间不变，总容量仍为 18,797,130 字节。快照：artifacts/overnight-library-after-20260908-0748.json。
- 最新样本 07:48:40：真实累计/提醒为 0，注入提醒为 1，验证窗口数为 0；无新增系统事件，尚未 finished。距计划结束约一小时，继续运行，不提前暂停跟进。

### 2026-09-08 08:49（北京时间）

- 正式日志已有 finished，实际墙钟 43,200.241 秒；正式进程已退出，主应用 PID 85568 仍存活。
- 最终图库快照保存为 artifacts/overnight-library-final-20260908.json；六类保持同一跨天新批次，无缺失、无再次更新。
- 已整理文首结论及人工验收边界；停止本轮自动跟进，不终止主应用。
