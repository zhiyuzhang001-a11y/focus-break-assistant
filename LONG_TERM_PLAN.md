# Focus Break Assistant 长期计划

> 状态：已采纳，作为项目长期推进依据  
> 建立日期：2026-08-30  
> 产品设计基线：[focus-break-assistant-design.md](/Users/zhiyu/Documents/Codex/2026-08-29/referenced-chatgpt-conversation-this-is-an/outputs/focus-break-assistant-design.md)

## 1. 长期目标

做出一款用户愿意长期保持运行的、隐私优先的 macOS 离屏休息助手：它只依据必要的本机活动与系统状态判断持续电脑使用，在真正合适的时机给出一次安静、不抢焦点、无需操作的提醒。

产品成功不等于提醒次数多，而等于：

- 错误时机足够少，用户信任它的判断；
- 自然休息能够可靠地取消不必要的提醒；
- 提醒看得清，但不迫使用户立即响应；
- 用户理解其隐私边界，并愿意让它持续运行；
- 软件稳定、低功耗，在低权限条件下也能合理降级。

## 2. 不可妥协的原则

1. 不读取输入内容、屏幕内容、网页内容、麦克风或摄像头。
2. 默认仅在本机处理必要信号；没有明确价值，不增加采集范围。
3. 不抢焦点、不发声、不阻塞操作、不要求确认。
4. 一个连续使用周期最多提醒一次；已经自然休息时不补发。
5. 不评价工作质量，不做效率评分、连续打卡或健康恐吓。
6. 宁可漏掉提醒，也不在明显错误的时机打断用户。
7. 新功能只有在核心提醒时机已经可信后才进入产品范围。

## 3. 当前需要先验证的关键假设

### H1：必要信号足够且权限成本可接受

只用 idle、会话切换、系统睡眠、显示器睡眠状态、活动屏幕与全屏状态，就能实现可接受的提醒时机；即使精确普通锁屏状态不可得或部分权限被拒绝，核心流程仍可通过 idle 延后和保守降级运行并清楚说明精度变化。

### H2：简单规则优于早期智能化

45 分钟累计、3 分钟短暂 idle、5 分钟自然休息和一次提醒规则，足以先验证产品价值，不需要 Focus Score、应用分类或 AI。

### H3：Horizon Gap 提醒兼顾可见与克制

固定位置、固定文案和 6–7 秒淡入淡出能够被余光感知，同时不被误认成系统通知、任务完成提示或必须操作的弹窗。

### H4：用户愿意长期运行

提醒偶尔出现且时机可信时，用户会保持自动启动，而不会因为打扰、权限疑虑、功耗或不可理解的行为而暂停或卸载。

## 4. 分阶段路线图

阶段按验收门槛推进，不以日期强行推进。上一阶段的关键风险未解决时，不扩大功能范围。

### 阶段 0：技术可行性探针

目标：先确认 macOS 信号、权限、全屏/多显示器和无焦点浮层的真实边界。

交付：

- 信号探针：idle、会话切换、系统睡眠、显示器状态、活动屏幕、全屏；普通锁屏边界需明确记录；
- 权限矩阵：每种信号所需权限、拒绝后的降级行为；
- 无焦点、点击穿透的临时浮层；
- 睡眠/唤醒、切换 Space、多显示器与系统时间变化的实验记录；
- 初步 CPU、内存和能源影响测量。

退出门槛：无需读取内容即可完成核心信号链；浮层不抢焦点、不拦截输入；关键权限被拒绝时不会反复索权或崩溃。

### 阶段 1：视觉提醒原型

目标：只验证 Horizon Gap 的可读性、辨识度和打扰程度，不接完整计时器。

交付：

- 深色、浅色、复杂壁纸与代码界面的视觉版本；
- 13/14 pt、不同屏幕尺寸、缩放和多显示器测试；
- 减少动态效果、增强对比度适配；
- “预览一次提醒”入口；
- 5–8 名试用者的结构化反馈。

退出门槛：所有测试环境中文字可读；无焦点与点击穿透测试全通过；多数试用者能理解“短暂离屏”意图，且不会误认成系统通知或任务完成。

### 阶段 2：行为引擎 MVP

目标：用确定性规则完成一个完整周期。

交付：

- `SignalCollector`、`FocusEngine`、`ReminderPolicy`、`OverlayPresenter`、`LocalStore` 五个模块；
- 单调时钟计时、tick 上限和崩溃恢复；
- 自然休息取消、全屏延后、同周期一次提醒；
- 用户暂停与系统暂停的明确区分；
- 状态转换表、单元测试和模拟时间测试。

退出门槛：设计稿中的核心测试场景全部自动化通过；睡眠、锁屏和应用重启不会补算时间或补发旧提醒；状态转换不存在重复提醒路径。

### 阶段 3：内部长期自用

目标：先由我们连续使用，验证真实一天中的行为，而不是只验证演示路径。

建议周期：至少 14 个实际使用日。

仅记录本机最小诊断事件：提醒为何显示、延后或取消，状态转换异常，崩溃与能源影响。默认不记录应用名称和用户内容，并提供一键清除。

每次提醒可在菜单栏事后标记“合适 / 不合适 / 没注意到”，但不在提醒浮层中增加交互。

退出门槛：

- 不出现抢焦点、阻塞点击、睡眠补算或同周期重复提醒；
- 自然休息后补发旧提醒为 0；
- 所有“不合适”提醒都有可解释原因并形成明确修复或接受决定；
- 没有持续高能源影响；
- 主观上仍愿意保持登录时自动运行。

### 阶段 4：小范围封闭测试

目标：验证规则是否只适合开发者本人，还是对不同工作方式也成立。

建议范围：10–20 名 macOS 用户，连续使用 2–4 周，覆盖写作、编程、设计、会议和多显示器场景。

退出门槛（首轮目标，测试后允许修订）：

- 至少 70% 的有效试用者愿意继续自动运行；
- 被明确标记为“不合适”的提醒低于 10%；
- 自然休息后的旧提醒补发为 0；
- 崩溃、重复索权、焦点抢占和输入阻塞均为发布阻断问题；
- 用户能够准确复述“检测活动、不读取内容”的隐私边界。

### 阶段 5：macOS MVP 发布

目标：发布一个范围克制、可长期运行的首版，而不是功能齐全的生产力套件。

发布必需：

- 首次启动说明、预览、登录时运行；
- 菜单栏暂停、预览、设置和本地数据清除；
- 完整权限说明和低权限降级；
- 自动化测试、真实设备回归、崩溃恢复和能源检查；
- 清晰的反馈与版本说明渠道。

发布后只优先处理：错误提醒、漏判自然休息、稳定性、功耗、可访问性和隐私理解问题。

### 阶段 6：证据驱动的增强

只有测试数据证明简单规则存在重复且重要的问题时，才依次评估：

1. 个性化的目标时长与自然休息阈值；
2. 会议、媒体等可可靠获取的弱信号；
3. 完全本机的规则自适应；
4. Windows 版本。

跨设备同步、云端账户和 AI 推断不设为默认路线；必须先提出无法由本机简单规则解决的明确问题，并重新评估隐私与维护成本。

## 5. 核心衡量方式

### 体验质量

- 不合适提醒率；
- 自然休息后错误补发次数；
- 同周期重复提醒次数；
- 提醒被误认为系统通知或任务完成的比例；
- 因打扰而暂停、关闭自动启动或卸载的比例；
- 用户是否愿意继续保持运行。

### 技术质量

- 焦点抢占、点击阻塞、异常发声次数；
- 状态机核心路径测试通过率；
- 睡眠、锁屏、唤醒和崩溃恢复错误；
- 崩溃率、CPU/内存与能源影响；
- 权限拒绝后的稳定性和解释清晰度。

提醒总数不是 KPI。零提醒可以是正确结果。

## 6. 在实现前补齐的规格

1. 用状态转换表明确每个“事件 × 当前状态”的下一状态和副作用；流程图只用于解释，转换表才作为实现依据。
2. 区分用户主动暂停与系统锁屏/睡眠，避免都塞进 `PAUSED` 后产生恢复歧义。
3. 明确定义 `idle < 3 分钟` 是否完整计入累计，以及跨过 3 分钟边界时是否回扣此前 idle；写成可测试的时间语义。
4. 定义应用退出、崩溃和系统重启后的持久化格式、版本迁移和损坏数据回退行为。
5. 定义活动屏幕与全屏检测失败时的默认策略：建议保守延后，但必须避免永久卡在 `DEFERRED`。
6. 为本地诊断记录设定明确字段、保留期、关闭与清除方式；“本地”不等于可以无限保存。
7. 把性能和能源影响列为验收项，避免后台常驻应用用高频轮询换取表面精度。
8. 文案避免暗示医疗效果；产品承诺限定为帮助形成离屏节律，而不是预防或治疗健康问题。

## 7. 明确暂缓与停止条件

以下情况出现时，暂停扩展功能，回到核心假设：

- 为获得基本体验必须申请与价值不相称的高敏感权限；
- 简单规则在不同用户中持续产生不可解释的错误提醒；
- 浮层无法在常见全屏、多显示器或辅助功能环境中可靠地不抢焦点；
- 后台检测造成明显能源影响；
- 用户理解隐私说明后仍普遍不愿长期运行。

如果两轮有针对性的迭代后，封闭测试仍无法达到继续运行意愿和错误提醒率目标，应重新审视产品机制，而不是增加更多视觉刺激、通知次数或智能化复杂度。

## 8. 决策方法

每个新增功能先回答：

1. 它解决了哪个已经观察到的问题？
2. 有多少用户、多少次场景受到影响？
3. 能否用更少信号、更低权限和更简单规则解决？
4. 它会不会增加错误提醒、打扰、功耗或隐私成本？
5. 如何验证有效，什么结果意味着撤回？

设计稿继续保存产品哲学、交互规范和完整决策记录；本文件只维护阶段、门槛、指标和当前优先级。两者冲突时，先记录冲突并做显式决策，不静默改写原则。

## 9. 当前唯一优先事项

完成“阶段 1：视觉提醒原型”。阶段 0 已在独立 8 小时资源采样通过后关闭；多显示器、新账户首次启动和正式 Energy Trace 作为发布前残余验证保留。Stage 1 按“候选收敛与用户决策 → 正式资产与原生实现 → 退出验收”三个独立任务推进。Stage 1 完成前不开始完整行为引擎、设置页或数据报表；在信号权限、无焦点浮层、全屏/多显示器和功耗边界有足够证据前，不开始 AI、跨设备或 Windows 开发。

具体顺序和完成条件见 [`docs/NEXT_EXECUTION_PLAN.md`](docs/NEXT_EXECUTION_PLAN.md)。
