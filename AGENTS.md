# 图片使用规则

- 不要自行生成图片；禁止调用 AI 图像生成、重绘或生成式编辑工具制作项目图片或概念稿。
- 允许用户提供的现成图片，以及许可明确的网上实拍照片、卡通和插画；保留来源与许可记录，不把用户导入素材的许可标为已核实。宇宙实拍与科幻插画不可混称。
- 本规则长期适用于整个项目，包括界面素材、预览和演示；除非用户明确修改本规则，不得恢复图片生成。

# 照片库与尺寸适配规则

- 以下是后续照片库功能的实现约束，不代表功能已经完成。
- 支持应用内置照片库和用户选择的本地照片文件夹；第一版支持 JPG/JPEG、PNG、HEIC。
- 内置照片优先选择横向实拍照片，16:9、3:2 均可；用户照片允许任意长宽比例，不要求预先裁剪或统一尺寸。
- 展示区域根据提醒所在屏幕的可用区域计算，展示区域随图片原始比例变化，最大宽 90%、最大高 85%，水平居中、顶部固定并采用窗帘展开方式；使用逻辑点适配 Retina，不以照片像素尺寸决定窗口大小。
- 始终保持照片原始比例，禁止拉伸、压扁或修改原始文件来适配屏幕。
- 默认先按图片比例计算展示区域，等比例完整显示，使常见横图、竖图、方图自然铺满各自区域；不再为所有图片固定一个宽高比。不要求竖图和超宽图同时占据屏幕面积一半。固定区域的兼容预览仍可使用下述 80% 自动模式；不生成补图。
- 自动选择可采用以下明确标准：铺满后保留的原图面积比例为 min(照片宽高比 / 展示区域宽高比, 展示区域宽高比 / 照片宽高比)；保留比例不低于 80% 时使用铺满，否则使用完整显示。
- 每张照片允许单独选择并记住“自动”“铺满裁切”“完整显示”；用户明确设置优先于自动判断，换屏幕后重新计算布局但保留用户选择。
- 铺满裁切围绕每张照片配置的重点位置进行；人物照片优先保留脸部。没有可靠主体位置时不假定已识别人脸，比例差异较大时优先完整显示。
- 完整显示时照片居中，底色从现有照片色彩中选取或使用中性柔和色；底色仅填充照片外留白，文字继续保持透明背景。
- 文字在展示区域中央自适应，字号保持 28–48 pt，保证对比度和边距，不随照片比例被拉伸或挤出画面。
- 每次提醒只展示一张照片，打乱顺序轮换；一轮用完再打乱，有至少两张可用照片时避免两轮交界重复。
- 用户文件夹为空、不可访问，或照片损坏、被移走时，跳过不可用照片并回退到内置库；不改动或删除用户原图。
- 验证需覆盖横图、竖图、方图、人物重点靠边，以及小屏、竖屏、超宽屏和多屏场景；分别检查自动、铺满和完整显示模式。

# 每日分类图库

- 六类：自然风景、宇宙星空、动物、城市、人物、卡通插画；支持多选、记忆偏好、独立导入。
- 每个本地自然日最多询问一次，关闭每日询问后仍可从菜单打开；满屏工作和不可用会话延后。
- 六类均支持在线来源：Commons 实拍、NASA 天文观测、Openclipart 现成卡通；保存作者、来源和许可，不生成图片。
- 用户已将在线规则改为每类固定 10 张，共 60 张；每个本地自然日检查，需更新时整批替换该类 10 张，成功前保留旧批。
- 每类不超过 20 MiB，常驻最多约 120 MiB；各类串行更新，仅额外暂存一类，正常峰值约 140 MiB。用户主动导入独立长期保留，不参与自动删除。
